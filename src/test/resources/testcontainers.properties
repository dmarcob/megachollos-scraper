# Ignore flaky user-level Testcontainers strategy overrides (e.g. ~/.testcontainers.properties)
# and auto-detect Docker settings from the current environment.
dockerconfig.source=autoIgnoringUserProperties

