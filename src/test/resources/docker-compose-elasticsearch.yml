version: '3'
services:
  elasticsearch:
    image: elasticsearch:8.17.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false   # Disable SSL
      - ES_JAVA_OPTS=-Xms750m -Xmx750m # Limit heap size